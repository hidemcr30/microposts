<section>
  <!-- bootstrapは後程
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
  -->
  
<% if current_user != @user %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
     <!-- bootstrapは後程
    </div>
  </div>
  -->
</section>


